[project]
name = "agent-guard"
version = "0.1.0"
description = "Runtime security proxy for MCP agent tool calls"
requires-python = ">=3.11"
dependencies = [
    # mcp 2.0 removed Server.list_tools/call_tool decorators used in proxy.py.
    # Pinned to the 1.x API until the 2.x migration lands.
    "mcp>=1.0.0,<2.0.0",
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "click>=8.0",
]

[project.scripts]
agent-guard = "agent_guard.cli:main"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent_guard"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
